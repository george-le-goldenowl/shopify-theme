{% layout none %}
{% capture output %}
    {% for p in search.results %}
        {"id": "{{ p.id }}", "title": "{{ p.title }}", "image": "{{ p.featured_image | image_url }}", "url": "{{ p.url }}", "options": {{ p.options | json }}, "variants": {{ p.variants | json }}}{% unless forloop.last %},{% endunless %}  
    {% endfor %}
{% endcapture %}
{"products":[{{ output | strip_newlines }}]}